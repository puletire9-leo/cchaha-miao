---
name: Claude Code 记忆系统结构
description: 记忆文件位置、加载写入逻辑分析
type: project
---

## 文件位置

| 路径 | 层级 | 加载情况 |
|------|------|----------|
| `.claude/MEMORY.md` | 项目级 | ✅ 新会话自动加载 |
| `.claude/memory/` | 项目级 | ✅ 已迁移 |
| `memory/` | 项目根 | ❌ 已废弃 |

## 加载逻辑

1. **新会话开始** → 读取 `.claude/MEMORY.md`
2. **MEMORY.md** 内容是索引，引用 `.claude/memory/` 中的 .md 文件
3. 被引用的文件内容加载为上下文

## 写入逻辑

- **手动写入**：调用 Write 工具
- **验证**：Write 成功返回文件路径
- **无自动写入**

## 当前状态

```
.claude/
├── MEMORY.md          ← 主索引（新会话加载）
└── memory/            ← ✅ 记忆文件
    ├── user_profile.md
    ├── 记忆规则_每次对话评估.md
    └── 记忆系统结构.md
```

## 迁移记录

- 2026-04-16 从 `memory/` 迁移到 `.claude/memory/`
